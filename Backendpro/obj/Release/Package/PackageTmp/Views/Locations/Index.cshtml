@model IEnumerable<Backendpro.Models.Location>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Locationid")
        </th>
        <th>
            @Html.DisplayName("Location")
        </th>
        <th>
            @Html.DisplayName("Location Code")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Locationid)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Location1)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.LocationCode)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.Locationid }) |
        @Html.ActionLink("Details", "Details", new { id = item.Locationid }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.Locationid })
    </td>
</tr>
}

</table>
